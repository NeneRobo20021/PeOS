# 食品仓储与每日饮食 · 开发文档

> 单人本地使用的「食品仓储 + 每日饮食记录与分析 + 运动记录与分析」系统。
> 本文档记录技术架构、已实现功能、已知限制与待开发项，供后续迭代参考。

---

## 1. 技术栈

| 层 | 技术 | 用途 |
|----|------|------|
| 前端框架 | Vue 3 | 组件化页面开发 |
| 构建工具 | Vite（`base: './'`） | 打包、热更新，产物用相对路径便于部署 |
| UI 组件库 | Element Plus | 表单/表格/菜单/进度条/日期选择器 |
| 图表 | ECharts (vue-echarts) | 趋势图、营养素饼图 |
| 路由 | Vue Router（hash 模式） | 页面跳转，适配静态托管 |
| HTTP | Axios | 连后端 REST API |
| 后端 | Node.js + Express | 本地中枢服务，监听 3000 端口 |
| 数据库 | SQLite (better-sqlite3) | 单文件库 `data/food_diet.db` |
| 图片 | 本地文件系统 `data/images/` | 购物小票、餐食照片 |
| 离线层 | IndexedDB + 前端同步逻辑 | 断网时浏览器暂存数据，恢复后同步 |

---

## 2. 架构总览

```
手机/电脑浏览器 ──HTTP/REST──▶ Node 服务(Express, :3000)
                                  │
                    ┌─────────────┼──────────────┐
                    ▼             ▼              ▼
              SQLite 数据库   图片文件目录    离线同步接口
           (data/food_diet.db) (data/images)  (/api/sync)
```

- **服务是中枢**：唯一拥有数据库、负责计算（BMR、热量汇总、库存扣减）、存图片。
- **前端是显示器**：通过 API 取数、提交操作。
- **`localhost:3000`** = 本机上的这个服务；同 WiFi 下手机用电脑局域网 IP 访问（如 `http://192.168.1.x:3000`）。
- **离线缓存（IndexedDB）**：仅在「已打开网页、中途掉线/电脑关机」时生效，数据暂存浏览器，重连后自动同步。

---

## 3. 目录结构

```
食品仓储与每日饮食/
├── server/                 # Node 后端
│   ├── index.js            # Express 入口，挂路由 + 托管前端
│   ├── db.js               # SQLite 初始化 + 建表
│   └── routes/
│       ├── foods.js        # 食品仓储 CRUD + 图片上传
│       ├── meals.js        # 三餐 + 关联食品 + 库存扣减
│       ├── snacks.js       # 零嘴 + 联动扣减
│       ├── exercises.js    # 运动记录 CRUD
│       ├── profile.js      # 用户档案（算 BMR）
│       ├── summary.js      # 热量计算核心（daily/trend/calorie-budget）
│       └── sync.js         # 离线同步接口
├── client/                 # Vue 前端
│   ├── src/
│   │   ├── main.js         # 入口，注册 Element Plus + 图标 + 路由
│   │   ├── App.vue         # 主框架（侧边栏 + 顶栏同步状态）
│   │   ├── router/         # 路由定义
│   │   ├── api/index.js    # Axios 封装
│   │   ├── utils/
│   │   │   ├── offline-db.js  # IndexedDB 离线缓存层
│   │   │   └── sync.js        # 在线检测 + 批量同步
│   │   ├── components/SyncStatus.vue  # 同步状态指示器
│   │   └── views/          # 各页面组件
│   │       ├── Dashboard.vue        # 仪表盘
│   │       ├── Foods.vue            # 食品仓储
│   │       ├── Meals.vue            # 一日三餐
│   │       ├── Snacks.vue           # 零嘴记录
│   │       ├── Exercises.vue        # 运动记录
│   │       ├── Profile.vue          # 个人档案
│   │       └── CalorieBudget.vue    # 热量开支（进度条）
│   └── vite.config.js      # 构建配置
├── data/                   # SQLite + 图片（已在 .gitignore 排除）
├── 启动服务.bat            # 双击启动后端（前台常驻）
└── .gitignore
```

---

## 4. 已实现功能

| 模块 | 功能 |
|------|------|
| 仪表盘 | 今日热量摄入/消耗/差额卡片 + 7日趋势图 + 营养素饼图 |
| 食品仓储 | 名称/价格/重量/营养成分表(能量·蛋白质·脂肪·碳水·钠)/配料表/已食用百分比 |
| 一日三餐 | 早/午/晚餐分类，照片上传，关联多项食品，文字评价 |
| 零嘴记录 | 即时购买/已有食品双模式，自动联动食品仓储扣减库存 |
| 运动记录 | 日期/类型/时长/里程/组数/卡路里/心率/配速全表单 |
| 个人档案 | 身高体重年龄性别健身目标 → 自动算 BMR |
| 热量开支页 | 双向收支进度条 + 分餐型摄入 + 消耗明细 + 运动列表 |
| 离线缓存 | 手机断网时数据暂存 IndexedDB，PC 上线后自动同步 |
| 同步状态 | 右上角实时显示 🟢在线 / 🟡待同步 / 🔄同步中 |

---

## 5. 数据库表

```
user_profile(个人信息) → 算 BMR
foods(食品仓储) → meal_foods → meals(三餐)
foods(食品仓储) → snacks(零嘴)
meals + snacks + exercises → daily_summary(每日热量汇总)
```

---

## 6. 启动方式

1. 双击 `启动服务.bat`（黑窗口常驻 = 服务运行中）
2. 浏览器打开 `http://localhost:3000`
3. 手机同 WiFi：浏览器打开 `http://电脑局域网IP:3000`

关掉黑窗口 = 停止服务。

---

## 7. 已知限制

- **跨网络不可用**：服务绑在局域网，手机不在同 WiFi 时网页打不开，IndexedDB 也无从缓存。
- **纯静态部署无数据**：前端若单独托管到 GitHub Pages，因无后端 API，只能看页面框架。
- **未实现项**：购物小票框定裁剪工具、配料表 OCR（Tesseract.js）尚未开发。

---

## 8. 待开发 / 架构决策

### 8.1 购物小票图片框定裁剪工具（优先级高）
- 接收一张购物小票照片 → 在图上手动框定多个食品区域 → 各自裁剪为独立图片 → 再绑定到食品项。
- 前端用 Canvas 实现框选 + 裁剪，无需后端。

### 8.2 配料表 OCR（Tesseract.js）
- 浏览器端识别配料表文字，辅以人工校正。不上传敏感图片。

### 8.3 PWA / Service Worker（已决策，暂未实现）
**背景**：用户希望「手机不在同 WiFi 下也能先记录、回家再同步」。
**当前结论**：现有 IndexedDB 离线缓存仅覆盖「已打开网页后中途掉线」场景；若手机从未连上过服务，网页本身打不开，无法缓存。
**决策方案**（待实施）：
1. 增加 Service Worker，首次在家 WiFi 打开时缓存 app 外壳（HTML/JS/CSS），使 app 在外地离线也能打开并录入。
2. 录入数据进 IndexedDB。
3. 回到能访问局域网服务的网络后，自动批量同步。
**硬限制（绕不开）**：数据最终同步目标仍是本机 3000 端口服务，故「真正同步回电脑」仍需手机回到可访问该局域网的网（家里 WiFi 或 VPN/内网穿透）。若要做到「外地记、外地即同步」，须将后端搬上公网，成本较高，单人自用暂不必要。
**状态**：2026-07-07 经讨论决定暂缓，先记录于本文档。
